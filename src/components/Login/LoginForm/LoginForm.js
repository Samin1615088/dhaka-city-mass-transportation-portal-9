import React from 'react';
import { Button, Container, Form } from 'react-bootstrap';
import './LoginForm.css';

const LoginForm = () => {
    return (
        <Container>
 
        <h2>This is login card</h2>
        </Container>
    );
};

export default LoginForm;